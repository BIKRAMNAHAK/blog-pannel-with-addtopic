<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/userprofile.css">

</head>

<body>

    <div class="container">
        <div class="profile-container">
            <img src="https://via.placeholder.com/150" alt="User Profile Picture" class="profile-pic">
            <div class="d-flex align-items-center gap-4">
                <h2>
                    <label class="fs-6 fw-bold">Name:</label>
                    <%= userDetails.name %>
                </h2>
                <a href="/change_profile/<%= userDetails._id %>" class="name-change text-decoration-none">Change Name</a>
            </div>

            <div class="user-info">
                <label>Email:</label>
                <span id="user-email">
                    <%= userDetails.email %>
                </span>
              
            </div>

            <div class="user-info">
                <label>Phone:</label>
                <span id="user-phone">
                    <%= userDetails.phone %>
                </span>
                <a href="/change_profile/<%= userDetails._id %>" class="edit-link text-decoration-none">Change Phone Number</a>
            </div>

            <div class="user-info">
                <label>Password:</label>
                <span>********</span>
                <a href="/change-password" class="edit-link text-decoration-none">Change Password</a>
            </div>
            <div class="d-flex gap-4">
                <a href="/" class="btn btn-info btn-logout text-decoration-none w-25">Home</a>
                <a href="/logout" class="btn btn-danger btn-logout text-decoration-none w-25">Logout</a>
            </div>
            
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>